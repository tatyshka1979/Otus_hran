## Скрипт разбивки на ветки

Запускать в терминале bash внутри папки репозитария

```bash
./SplitBranchExtended.sh
```

## Скрипт очистки от всех веток из папки features 
Еще дополнительно отменяет ветку branch_sync_hran до коммита Develop с надписью "Создание хранилища конфигурации"

```bash
./DeleteBranch.sh
```